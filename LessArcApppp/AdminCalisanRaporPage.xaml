<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Name="__page"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:microcharts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:shapes="clr-namespace:Microsoft.Maui.Controls.Shapes;assembly=Microsoft.Maui.Controls"
    xmlns:controls="clr-namespace:LessArcApppp.Controls"
    x:Class="LessArcApppp.AdminCalisanRaporPage"
    Title=""
    BackgroundColor="Transparent"
    ios:Page.UseSafeArea="False"
    Padding="0"
    NavigationPage.HasNavigationBar="False"
    Shell.NavBarIsVisible="False">

    <!-- ÜST BAR + GÖVDE -->
    <Grid RowDefinitions="Auto,*" Padding="0">

        <!-- 🔹 ÜST BAR (arka plan #5D5A56, başlık yok, yalnızca geri ok) -->
        <controls:CustomTitleBar
            Grid.Row="0"
            Title=""
            ShowBack="True">
            <controls:CustomTitleBar.Resources>
                <!-- İstersen TitleBar'ı tamamen aynı renge sabitlemek için -->
            </controls:CustomTitleBar.Resources>
        </controls:CustomTitleBar>

        <!-- 🔹 GÖVDE -->
        <Grid Grid.Row="1"
              Padding="0"
              ColumnDefinitions="2*,3*"
              RowDefinitions="*"
              RowSpacing="{Binding ScaledSpacing}"
              ColumnSpacing="{Binding ScaledSmallSpacing}">

            <!-- Arka Plan (gövdeyi kaplasın) -->
            <Image Source="arkaplann.png"
                   Aspect="AspectFill"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill"
                   IsEnabled="False"
                   Grid.ColumnSpan="2" />

            <!-- ===== MASAÜSTÜ (SOL) — LİSTE ===== -->
            <Grid Grid.Row="0" Grid.Column="0"
                  Margin="{Binding M_PageOuter}"
                  RowDefinitions="Auto,*"
                  RowSpacing="{Binding ScaledSmallSpacing}"
                  IsVisible="{OnPlatform WinUI=True, MacCatalyst=True, Android=False, iOS=False}">
                <Grid Grid.Row="0" RowDefinitions="Auto,Auto">
                    <Label Text="ÇALIŞANLAR"
                           FontAttributes="Bold"
                           FontSize="{Binding ScaledFontSize2}"
                           TextColor="#dcd2ca"
                           Margin="2,0,2,6"/>
                    <BoxView Grid.Row="1" HeightRequest="1"
                             Opacity="0.15"
                             BackgroundColor="#2B302E"/>
                </Grid>

                <CollectionView Grid.Row="1"
                                x:Name="calisanListesi"
                                ItemsLayout="VerticalList"
                                VerticalOptions="FillAndExpand"
                                Margin="0"
                                BackgroundColor="#00000000"
                                SelectionMode="Single"
                                SelectionChanged="calisanListesi_SelectionChanged">
                    <CollectionView.Header>
                        <Grid Padding="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallPadding}">
                            <Label Text="Takımdaki tüm çalışanlar"
                                   FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                   TextColor="#B6B0AA"/>
                        </Grid>
                    </CollectionView.Header>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame
                                Margin="{Binding Source={x:Reference __page}, Path=BindingContext.M_ItemBetween}"
                                Padding="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallPadding}"
                                BackgroundColor="#F9F7F5"
                                BorderColor="#E3DDD6"
                                CornerRadius="14"
                                HasShadow="True">
                                <Grid ColumnDefinitions="Auto,*,Auto"
                                      RowDefinitions="Auto,Auto"
                                      ColumnSpacing="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallSpacing}"
                                      RowSpacing="2">
                                    <Grid WidthRequest="40" HeightRequest="40"
                                          VerticalOptions="Center"
                                          HorizontalOptions="Center">
                                        <shapes:Ellipse Fill="#E8E2DB" Stroke="#D8D2CB" StrokeThickness="1.2"/>
                                        <Label Text="👤" FontSize="18"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                    </Grid>

                                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                        <Label Text="{Binding AdSoyad}"
                                               FontAttributes="Bold"
                                               FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize3}"
                                               TextColor="#2F4149"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="1"/>
                                        <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8">
                                            <Frame Padding="8,2"
                                                   BackgroundColor="#EDF5F2"
                                                   BorderColor="#D6E7E0"
                                                   CornerRadius="8"
                                                   HasShadow="False">
                                                <Label Text="{Binding ToplamProjeSayisi, StringFormat='Projeler: {0}'}"
                                                       FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                                       TextColor="#3A5F55"/>
                                            </Frame>
                                            <Label Grid.Column="1"
                                                   Text="Detay görmek için seçin"
                                                   FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                                   TextColor="#8A8784"
                                                   VerticalTextAlignment="Center"/>
                                        </Grid>
                                    </VerticalStackLayout>

                                    <Grid Grid.Column="2" VerticalOptions="Center">
                                        <Label Text="›"
                                               FontAttributes="Bold"
                                               FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize3}"
                                               TextColor="#8B877F"/>
                                    </Grid>

                                    <BoxView Grid.Row="1" Grid.ColumnSpan="3"
                                             HeightRequest="0.8"
                                             Opacity="0.07"
                                             BackgroundColor="#2B302E"
                                             Margin="0,8,0,0"/>
                                </Grid>

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroupList>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal">
                                                <VisualState.Setters>
                                                    <Setter Property="BackgroundColor" Value="#F9F7F5" />
                                                    <Setter Property="BorderColor" Value="#E3DDD6" />
                                                    <Setter Property="Scale" Value="1" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="PointerOver">
                                                <VisualState.Setters>
                                                    <Setter Property="BackgroundColor" Value="#FFFFFF"/>
                                                    <Setter Property="BorderColor" Value="#D4CDC5"/>
                                                    <Setter Property="Scale" Value="1.01"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Selected">
                                                <VisualState.Setters>
                                                    <Setter Property="BackgroundColor" Value="#FFF7ED"/>
                                                    <Setter Property="BorderColor" Value="#E9CFAF"/>
                                                    <Setter Property="Scale" Value="1.0"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Disabled">
                                                <VisualState.Setters>
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateGroupList>
                                </VisualStateManager.VisualStateGroups>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.Footer>
                        <Grid HeightRequest="8"/>
                    </CollectionView.Footer>
                </CollectionView>
            </Grid>

            <!-- ===== MASAÜSTÜ (SAĞ) ===== -->
            <ScrollView Grid.Row="0" Grid.Column="1"
                        Margin="{Binding M_PageOuterRight}"
                        IsVisible="{OnPlatform WinUI=True, MacCatalyst=True, Android=False, iOS=False}">
                <VerticalStackLayout Spacing="{Binding ScaledSpacing}">
                    <Frame BackgroundColor="#f2efeb" CornerRadius="16"
                           Padding="{Binding ScaledPadding}" HasShadow="True" BorderColor="#CFCAC4">
                        <VerticalStackLayout Spacing="{Binding ScaledSmallSpacing}" HorizontalOptions="Center">
                            <Label Text="📊 Proje Durum Grafiği"
                                   FontSize="{Binding ScaledFontSize3}"
                                   FontAttributes="Bold"
                                   FontFamily="EmojiFont"
                                   TextColor="#2B302E" />
                            <microcharts:ChartView x:Name="chartView"
                                                   HeightRequest="{Binding ChartHeight}"
                                                   WidthRequest="{Binding ChartWidth}"
                                                   HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame BackgroundColor="#f2efeb" CornerRadius="16"
                           Padding="{Binding ScaledSmallPadding}" HasShadow="True" BorderColor="#CFCAC4">
                        <VerticalStackLayout Spacing="{Binding ScaledSmallSpacing}">
                            <Grid ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center">
                                <Button x:Name="btnAyOnceki" Text="‹"
                                        WidthRequest="{Binding NavBtnSize}"
                                        HeightRequest="{Binding NavBtnSize}"
                                        TextColor="#2B302E" FontAttributes="Bold"
                                        FontSize="{Binding NavChevronSize}"
                                        Clicked="btnAyOnceki_Clicked" />
                                <Label x:Name="lblAyBaslik" Grid.Column="1"
                                       HorizontalOptions="Center"
                                       FontAttributes="Bold"
                                       FontSize="{Binding ScaledFontSize3}"
                                       TextColor="#2B302E"
                                       Text="AĞUSTOS 2025" />
                                <Button x:Name="btnAySonraki" Grid.Column="2" Text="›"
                                        WidthRequest="{Binding NavBtnSize}"
                                        HeightRequest="{Binding NavBtnSize}"
                                        TextColor="#2B302E" FontAttributes="Bold"
                                        FontSize="{Binding NavChevronSize}"
                                        Clicked="btnAySonraki_Clicked" />
                            </Grid>
                            <Label Text="📈 Aylık Proje İlerlemesi"
                                   FontSize="{Binding ScaledFontSize3}"
                                   FontAttributes="Bold"
                                   TextColor="#2B302E" />
                            <microcharts:ChartView x:Name="lineChartView"
                                                   HeightRequest="{Binding ChartHeight}" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame BackgroundColor="#F6F3F2" CornerRadius="16"
                           Padding="{Binding ScaledPadding}" HasShadow="True" BorderColor="#CFCAC4">
                        <VerticalStackLayout Spacing="{Binding ScaledSmallSpacing}">
                            <Label Text="📄 Çalışan Detayları"
                                   FontSize="{Binding ScaledFontSize2}"
                                   FontAttributes="Bold" FontFamily="EmojiFont" TextColor="#2B302E" />
                            <Label x:Name="lblAdSoyad"    FontSize="{Binding ScaledFontSize3}" TextColor="#2B302E" />
                            <Label x:Name="lblToplam"     FontSize="{Binding ScaledFontSize3}" TextColor="#2B302E" />
                            <Label x:Name="lblDevam"      FontSize="{Binding ScaledFontSize3}" TextColor="#2B302E" />
                            <Label x:Name="lblTamam"      FontSize="{Binding ScaledFontSize3}" TextColor="#2B302E" />
                            <Label x:Name="lblBaslamayan" FontSize="{Binding ScaledFontSize3}" TextColor="#2B302E" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame BackgroundColor="#F6F3F2" CornerRadius="16"
                           Padding="{Binding ScaledSmallPadding}" HasShadow="True" BorderColor="#CFCAC4">
                        <VerticalStackLayout Spacing="{Binding ScaledSmallSpacing}">
                            <Label Text="🛠️ Projeler"
                                   FontSize="{Binding ScaledFontSize3}"
                                   FontAttributes="Bold" FontFamily="EmojiFont" TextColor="#2B302E" />
                            <CollectionView x:Name="projeDetayListesi" IsVisible="False" SelectionMode="None">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame BackgroundColor="#F6F3F2" BorderColor="#CFCAC4" CornerRadius="10"
                                               Padding="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallPadding}"
                                               Margin="{Binding Source={x:Reference __page}, Path=BindingContext.M_ItemBetween}">
                                            <Grid RowDefinitions="Auto,Auto,Auto"
                                                  RowSpacing="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallSpacing}">
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Text="{Binding Baslik}"
                                                           FontAttributes="Bold"
                                                           FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize3}"
                                                           TextColor="#2B302E" />
                                                    <Frame Grid.Column="1"
                                                           Padding="10,4"
                                                           CornerRadius="10"
                                                           HasShadow="False"
                                                           HorizontalOptions="End"
                                                           VerticalOptions="Center"
                                                           BackgroundColor="#EAF6FF"
                                                           BorderColor="Transparent">
                                                        <Label Text="{Binding Aciklama}"
                                                               FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                                               TextColor="#0B6B99">
                                                            <Label.Triggers>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Devam Ediyor">
                                                                    <Setter Property="TextColor" Value="#A36B00"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Devam">
                                                                    <Setter Property="TextColor" Value="#A36B00"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Başlamadı">
                                                                    <Setter Property="TextColor" Value="#9B1C1C"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Baslamadi">
                                                                    <Setter Property="TextColor" Value="#9B1C1C"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Tamamlandı">
                                                                    <Setter Property="TextColor" Value="#1B5E20"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Tamamlanmış">
                                                                    <Setter Property="TextColor" Value="#1B5E20"/>
                                                                </DataTrigger>
                                                            </Label.Triggers>
                                                        </Label>
                                                    </Frame>
                                                </Grid>

                                                <Grid Grid.Row="1" ColumnDefinitions="*,Auto"
                                                      ColumnSpacing="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledSmallSpacing}">
                                                    <Label Text="{Binding BaslangicTarihi, StringFormat='Başlangıç: {0:dd.MM.yyyy}'}"
                                                           FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                                           TextColor="#6D6A68" />
                                                    <Label Grid.Column="1"
                                                           Text="{Binding BitisTarihi, StringFormat='Bitiş: {0:dd.MM.yyyy}'}"
                                                           FontSize="{Binding Source={x:Reference __page}, Path=BindingContext.ScaledFontSize4}"
                                                           TextColor="#6D6A68" />
                                                </Grid>

                                                <BoxView Grid.Row="2" HeightRequest="0.5" BackgroundColor="#2B302E" Opacity="0.08" />
                                            </Grid>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>
                </VerticalStackLayout>
            </ScrollView>

            <!-- ===== MOBİL ===== -->
            <ScrollView Grid.Row="0" Grid.ColumnSpan="2"
                        Padding="0"
                        BackgroundColor="Transparent"
                        IsVisible="{OnPlatform Android=True, iOS=True, WinUI=False, MacCatalyst=False}"
                        HorizontalOptions="Fill" VerticalOptions="Fill">
                <VerticalStackLayout Padding="10,12" Spacing="8"
                                     HorizontalOptions="FillAndExpand"
                                     VerticalOptions="Start">

                    <!-- Başlık & Çalışan seçimi -->
                    <VerticalStackLayout Spacing="6" Margin="0">
                        <Label Text="Çalışan Seçin"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#D6C9C1"
                               HorizontalOptions="Center" />
                        <Grid ColumnDefinitions="*" Padding="0">
                            <Picker x:Name="pickerCalisanMobile"
                                    Title="Çalışan Seçin"
                                    ItemDisplayBinding="{Binding AdSoyad}"
                                    SelectedIndexChanged="pickerCalisanMobile_SelectedIndexChanged"
                                    BackgroundColor="#F6F3F2"
                                    TextColor="#2B302E"
                                    FontSize="14"
                                    HeightRequest="44"
                                    HorizontalOptions="FillAndExpand" />
                        </Grid>
                    </VerticalStackLayout>

                    <!-- Durum Grafiği -->
                    <Frame BackgroundColor="#F6F3F2" CornerRadius="14"
                           Padding="12" HasShadow="True" BorderColor="#CFCAC4">
                        <Grid RowDefinitions="Auto,*">
                            <Label Text="📊 Proje Durum Grafiği"
                                   FontSize="14" FontAttributes="Bold"
                                   FontFamily="EmojiFont" TextColor="#2B302E"
                                   HorizontalOptions="Start" Margin="10,0,0,8" />
                            <microcharts:ChartView x:Name="chartViewMobile"
                                                   Grid.Row="1"
                                                   WidthRequest="260"
                                                   HeightRequest="200"
                                                   HorizontalOptions="Start"
                                                   VerticalOptions="Center"
                                                   Margin="10,0,0,0" />
                        </Grid>
                    </Frame>

                    <!-- Aylık İlerleme -->
                    <Frame BackgroundColor="#F6F3F2" CornerRadius="14"
                           Padding="10" HasShadow="True" BorderColor="#CFCAC4" Margin="0,0,0,6">
                        <VerticalStackLayout Spacing="6">
                            <Grid ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center">
                                <Button x:Name="btnAyOncekiMobile"
                                        Text="‹" WidthRequest="32" HeightRequest="32" Padding="0"
                                        Clicked="btnAyOnceki_Clicked" />
                                <Label x:Name="lblAyBaslikMobile"
                                       Grid.Column="1" HorizontalOptions="Center"
                                       FontAttributes="Bold" FontSize="14"
                                       TextColor="#2B302E" Text="AĞUSTOS 2025" />
                                <Button x:Name="btnAySonrakiMobile"
                                        Grid.Column="2" Text="›"
                                        WidthRequest="32" HeightRequest="32" Padding="0"
                                        Clicked="btnAySonraki_Clicked" />
                            </Grid>

                            <Label Text="📈 Aylık Proje İlerlemesi"
                                   FontSize="13" FontAttributes="Bold"
                                   TextColor="#2B302E" HorizontalOptions="Center" />

                            <microcharts:ChartView x:Name="lineChartViewMobile"
                                                   HeightRequest="220"
                                                   WidthRequest="380"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center"
                                                   Margin="-6,0" />
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Çalışan Bilgileri -->
                    <Frame BackgroundColor="#F6F3F2" CornerRadius="14"
                           Padding="14" HasShadow="True" BorderColor="#CFCAC4" Margin="0,0,0,6">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="👤 Çalışan Bilgileri"
                                   FontSize="14" FontAttributes="Bold"
                                   TextColor="#2B302E" HorizontalOptions="Center"/>
                            <Label x:Name="lblAdSoyadMobile"    FontSize="14" TextColor="#2B302E" />
                            <Label x:Name="lblToplamMobile"     FontSize="14" TextColor="#2B302E" />
                            <Label x:Name="lblDevamMobile"      FontSize="14" TextColor="#2B302E" />
                            <Label x:Name="lblTamamMobile"      FontSize="14" TextColor="#2B302E" />
                            <Label x:Name="lblBaslamayanMobile" FontSize="14" TextColor="#2B302E" />
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Proje Detayları -->
                    <Label Text="🧩 Projeler"
                           FontSize="20" FontAttributes="Bold"
                           TextColor="#dcd2ca" Margin="0,6,0,0"
                           HorizontalOptions="Center" />
                    <CollectionView x:Name="projeDetayListesiMobile"
                                    IsVisible="False" SelectionMode="None"
                                    Margin="0,2,0,10">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Padding="12" CornerRadius="14"
                                       HasShadow="True" BackgroundColor="#FFFFFF"
                                       BorderColor="#E4E0DB" Margin="0,0,0,2">
                                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="6">
                                        <Grid ColumnDefinitions="*,Auto">
                                            <Label Text="{Binding Baslik}"
                                                   FontAttributes="Bold" FontSize="15"
                                                   TextColor="#2B302E"
                                                   LineBreakMode="TailTruncation" MaxLines="2" />
                                            <Frame Grid.Column="1" Padding="10,4" CornerRadius="10"
                                                   HasShadow="False" HorizontalOptions="End"
                                                   VerticalOptions="Center" BackgroundColor="#EAF6FF"
                                                   BorderColor="Transparent">
                                                <Label Text="{Binding Aciklama}"
                                                       FontSize="12" TextColor="#0B6B99">
                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Devam Ediyor">
                                                            <Setter Property="TextColor" Value="#A36B00"/>
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Devam">
                                                            <Setter Property="TextColor" Value="#A36B00"/>
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Başlamadı">
                                                            <Setter Property="TextColor" Value="#9B1C1C"/>
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Baslamadi">
                                                            <Setter Property="TextColor" Value="#9B1C1C"/>
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Tamamlandı">
                                                            <Setter Property="TextColor" Value="#1B5E20"/>
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding Aciklama}" Value="Tamamlanmış">
                                                            <Setter Property="TextColor" Value="#1B5E20"/>
                                                        </DataTrigger>
                                                    </Label.Triggers>
                                                </Label>
                                            </Frame>
                                        </Grid>

                                        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" RowDefinitions="Auto" ColumnSpacing="10">
                                            <Label Text="{Binding BaslangicTarihi, StringFormat='Başlangıç: {0:dd.MM.yyyy}'}"
                                                   FontSize="12" TextColor="#6D6A68" />
                                            <Label Grid.Column="1"
                                                   Text="{Binding BitisTarihi, StringFormat='Bitiş: {0:dd.MM.yyyy}'}"
                                                   FontSize="12" TextColor="#6D6A68" />
                                        </Grid>

                                        <BoxView Grid.Row="2" HeightRequest="0.5" BackgroundColor="#2B302E" Opacity="0.08" />
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </ScrollView>

        </Grid>
    </Grid>
</ContentPage>
